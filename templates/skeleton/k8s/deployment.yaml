apiVersion: apps/v1
kind: Deployment
metadata:
  name: ranjeet-eduarn-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ranjeet-eduarn-app
  template:
    metadata:
      labels:
        app: ranjeet-eduarn-app
    spec:
      containers:
        - name: app
          image: nginx
          ports:
            - containerPort: 80
